// eli brooks
// 1/25/22
// animation and conditional operators

// setup
function setup() {
    frameRate(1);
}

// call the draw function so numbers will start in the log
function draw() {
    console.log(frameCount);
}